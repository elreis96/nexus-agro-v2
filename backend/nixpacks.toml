providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]
aptPkgs = []

[phases.install]
cmds = [
  "cd api && pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd api && python -m uvicorn index:app --host 0.0.0.0 --port ${PORT:-8000}"
